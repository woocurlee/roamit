# Copy to .env.local and fill in real values.

# --- Backend (roamit-api) ---
# 로컬 개발 시 roamit-api 기본 주소. roamit-api 는 CORS_ORIGIN 환경변수에
# 이 프론트엔드 주소(기본 http://localhost:3000)가 등록되어 있어야 요청을 허용합니다.
NEXT_PUBLIC_API_URL="http://localhost:3001"
